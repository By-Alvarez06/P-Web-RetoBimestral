\documentclass{article}
\usepackage[utf8]{inputenc}
% AQUÍ ESTÁ LA SOLUCIÓN: Agregamos es-noquoting
\usepackage[spanish,es-noquoting]{babel} 
\usepackage[landscape, margin=1in]{geometry}
\usepackage{tikz}
\usepackage{graphicx} 
% Ya no necesitamos 'babel' aquí, la eliminamos de la lista
\usetikzlibrary{shapes.multipart, positioning, arrows.meta, calc, shadows}

\begin{document}

\begin{center}
\textbf{\Large Diagrama de Clases: Plataforma B2B2C}\vspace{1cm}

\noindent\resizebox{\textwidth}{!}{
\begin{tikzpicture}[
  font=\sffamily\small,
  >=Stealth,
  class/.style={
    rectangle split,
    rectangle split parts=3,
    draw,
    thick,
    fill=white,
    text width=4.5cm,
    align=center,
    drop shadow={opacity=0.1, shadow xshift=2pt, shadow yshift=-2pt}
  },
  line/.style={draw, thick}
]

  % =====================
  % DEFINICIÓN DE CLASES
  % =====================
  
  % --- Fila Central (Eje principal de izquierda a derecha) ---
  \node[class] (Pedido) {
    \textbf{Pedido}
    \nodepart{two}
    + id: Integer\\
    + fecha: DateTime\\
    + estado: String\\
    + monto\_total\_tienda: Float
    \nodepart{three}
    + confirmarPedido()\\
    + calcularTotal()
  };

  \node[class, left=1cm of Pedido] (Tienda) {
    \textbf{Tienda}
    \nodepart{two}
    + id: Integer\\
    + nombre: String\\
    + direccion: String
    \nodepart{three}
  };

  \node[class, right=1.5cm of Pedido] (DetallePedido) {
    \textbf{DetallePedido}
    \nodepart{two}
    + id: Integer\\
    + cantidad: Integer\\
    + precio\_unitario: Float\\
    + subtotal: Float
    \nodepart{three}
    + calcularSubtotal()
  };

  \node[class, right=1.5cm of DetallePedido] (Producto) {
    \textbf{Producto}
    \nodepart{two}
    + id: Integer\\
    + nombre: String\\
    + precio\_mayorista: Float
    \nodepart{three}
  };

  \node[class, right=1.5cm of Producto] (Inventario) {
    \textbf{Inventario}
    \nodepart{two}
    + id: Integer\\
    + cantidad\_disp: Integer\\
    + version: Integer [Lock]
    \nodepart{three}
    + descontarStock()\\
    + verificarConcurrencia()
  };

  % --- Fila Superior ---
  \node[class, above=2cm of Pedido] (Vendedor) {
    \textbf{Vendedor}
    \nodepart{two}
    + puntos\_acumulados: Integer\\
    + presupuesto\_ventas: Float
    \nodepart{three}
    + canjearPuntos()
  };

  \node[class, above=2cm of Producto] (Comercializadora) {
    \textbf{Comercializadora}
    \nodepart{two}
    + nombre\_empresa: String\\
    + suscripcion\_activa: Boolean
    \nodepart{three}
    + renovarSuscripcion()
  };

  % Centramos al Usuario dinámicamente entre Vendedor y Comercializadora
  \node[class] (Usuario) at ($(Vendedor)!0.5!(Comercializadora) + (0, 3cm)$) {
    \textbf{Usuario}
    \nodepart{two}
    + id: Integer\\
    + ruc: String\\
    + email: String\\
    + password: String\\
    + rol: String
    \nodepart{three}
    + autenticar()\\
    + actualizarDatos()
  };

  % --- Fila Inferior ---
  \node[class, below=2cm of Pedido] (Liquidacion) {
    \textbf{LiquidacionComercializadora}
    \nodepart{two}
    + id: Integer\\
    + monto\_comision: Float\\
    + monto\_cobrar: Float\\
    + fecha\_liquidacion: DateTime
    \nodepart{three}
    + calcularFee()
  };

  \node[class, left=1cm of Liquidacion] (Transaccion) {
    \textbf{TransaccionPuntos}
    \nodepart{two}
    + id: Integer\\
    + puntos\_ganados: Integer\\
    + fecha: DateTime
    \nodepart{three}
    + registrarTransaccion()
  };

  \node[class, below=2cm of Producto] (Campana) {
    \textbf{CampanaRecompensa}
    \nodepart{two}
    + id: Integer\\
    + factor\_puntos: Integer\\
    + fecha\_inicio: DateTime\\
    + fecha\_fin: DateTime
    \nodepart{three}
    + esVigente()
  };

  % =====================
  % RELACIONES Y FLECHAS
  % =====================
  
  % Herencias (Triángulo blanco hacia Usuario)
  \draw[-{Triangle[open, scale=1.5]}, line] (Vendedor.north) -- ++(0,0.5) -| (Usuario.south);
  \draw[-{Triangle[open, scale=1.5]}, line] (Comercializadora.north) -- ++(0,0.5) -| (Usuario.south);

  % Asociaciones verticales
  \draw[-Stealth, line] (Vendedor.south) -- (Pedido.north) node[midway, right] {registra 1..*};
  \draw[-Stealth, line] (Comercializadora.south) -- (Producto.north) node[midway, left] {posee 1..*};
  
  % Eje horizontal de pedidos y productos
  \draw[-Stealth, line] (Tienda.east) -- (Pedido.west) node[midway, above] {recibe 1..*};
  \draw[{Diamond[fill=black]}-Stealth, line] (Pedido.east) -- (DetallePedido.west) node[midway, above] {contiene 1..*};
  \draw[-Stealth, line] (DetallePedido.east) -- (Producto.west) node[midway, above] {incluye 1..1};
  \draw[{Diamond[fill=black]}-Stealth, line] (Producto.east) -- (Inventario.west) node[midway, above] {tiene 1..1};

  % Relaciones inferiores
  \draw[-Stealth, line] (Pedido.south) -- (Liquidacion.north) node[midway, right] {liquida 1..1};
  \draw[-Stealth, line] (Pedido.south) ++(-1.5cm,0) |- (Transaccion.east) node[near start, left] {genera 1..1};
  \draw[-Stealth, line] (Producto.south) -- (Campana.north) node[midway, right] {aplica 0..*};

\end{tikzpicture}
}
\end{center}

\end{document}